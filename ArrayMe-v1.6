#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main() {
	
char letters[12] = {"BDZXWMOSPHGE"}, toSearch, toDelete, found, temp;
int size = 12,choice, ch, i, j, pos;

	do
	{
		printf("\nMenu\n\n");
		printf("1. Search the Array\n");
		printf("2. Delete from the Array\n");
		printf("3. Sort the content of the Array\n");
		printf("4. Exit\n");
		printf("Choose by typing the number: \n");
		if (scanf("%ls", &choice) != 1)
            break;


		switch(choice)
		{
			case '1': {
 
    		printf("Input your letter to search:\n");
        	scanf(" %c", &toSearch);

    		found = 0;

			    for(i=0; i < size; i++){

    			    if (letters[i] == toSearch){

            			found = 1;
           			break;
    			    }
    			}

			    if(found == 1){

        			printf("\nInput : %c\n", toSearch);
        			printf("%c found at position %d", toSearch, i+1);
    			}

    			else{

    			    printf("Input not found in the array.");
    			}
    			
            	while ((ch = getchar()) != '\n' && ch != EOF) ;
				printf("\n\nPress ENTER to continue.");
				while ((ch = getchar()) != '\n' && ch != EOF) ;
            }
				break;
			case '2': {
                printf("Before deletion:\n");
                    for (int i = 0;i < size; i++)  {
                        printf("%c ", letters[i]);
                }
                printf("\nInput your letter to delete: \n");
                    scanf(" %c", &toDelete);

                found = 0;

                printf("\nLetter to delete: %c\n", toDelete);
                for (i=0; i < size; i++) {
                    if(letters[i] == toDelete)
                    {
                        found = 1;
                        pos = i;
                        break;
                    }
                }
                if(found == 1) {

                    for(i = pos; i < size; i++)
                        letters[i] = letters[i+1];
                		size = size - 1;
                }
                
            	else if ((i == (size - 1))) {
				}                
                else
                    printf("\n\nElement %c is not found in the array\n\n", toDelete);

                printf("\nArray Content:\n");

                for(i = 0; i < size; i++)
                    printf("%c ", letters[i]);
				while ((ch = getchar()) != '\n' && ch != EOF) ;
				printf("\n\nPress ENTER to continue.");
				while ((ch = getchar()) != '\n' && ch != EOF) ;
            }
				break;
			case '3': {
				printf("\nBefore sorting:\n");
        			for (int i = 0; i <size; i++)  {
            			printf("%c ", letters[i]);
   				}
			    printf("\n");
    
				for (i = 0; i < size-1; i++) {
					for (j = i+1; j < size; j++) {
						if (letters[i] > letters[j]) {
							temp = letters[i];
							letters[i] = letters[j];
        					letters[j] = temp;
    			    	}
					}
				}
				
				printf("\nSorted Array:\n");
				for (i = 0; i < size; i++)
					printf("%c ", letters[i]);
				printf("\n");
				
				while ((ch = getchar()) != '\n' && ch != EOF) ;
				printf("\n\nPress ENTER to continue.");
				while ((ch = getchar()) != '\n' && ch != EOF) ;
			}
				break;
			case '4': printf("Quitting program!\n");
				exit(0);
				break;
			default: printf("Invalid input!\n");
                break;
		}

	} while (choice!= '4');
;
	return 0;
}
